{
  "name": "sistema-5s-lean",
  "version": "1.0.0",
  "private": true,
  "description": "Portal 5S & Qualidade IMPAK TTO - Frontend + Netlify Functions + Neon Postgres",
  "scripts": {
    "dev": "node --env-file=.env dev-server.mjs",
    "start": "node --env-file=.env dev-server.mjs",
    "seed": "node --env-file=.env scripts/seed-official-users.mjs",
    "migrate": "node --env-file=.env scripts/migrate-jsonblob-to-neon.mjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2"
  }
}
